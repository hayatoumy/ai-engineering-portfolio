# AI Engineering Portfolio

## Goal

Deployed, end to end AI engineering projects; RAG, agents, and production APIs.

## Structure

- `packages/` — reusable libraries (e.g. `llmkit`)
- `projects/` — deployed applications

## Setup

```bash
uv sync --all-packages
```
